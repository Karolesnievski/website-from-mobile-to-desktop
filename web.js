const x_close = addEventListener('.fa-solid');
const nav__href = addEventListener('.main__href');
const second__main = addEventListener('.second__main');

function openNav() {
document.getElementsByClassName('main__href')
}